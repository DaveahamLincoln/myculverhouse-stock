<h1>Programs Index</h1>

<%if current_user.godBit or current_user.isSuperUser%>
    <table border=1>
      <tr>
        <th>Name</th>
        <th></th>
        <th></th>
      </tr>

    <% @programs.each do |program| %>
      <tr>
        <td><%= program.name %></td>
        <td><%= link_to("Homepage",Cms::Site.find(program.cms_site_id).path)%></td>
        <td><%= link_to 'Destroy', program, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
    </table>

    <br />

    <%= link_to 'New program', new_program_path %>

<%else%>

  <table border=1>
    <tr>
      <th>Name</th>
      <th></th>
    </tr>

  <% @programs.each do |program| %>
    <tr>
      <td><%= program.name %></td>
      <td><%= link_to("Homepage",Cms::Site.find(program.cms_site_id).path)%></td>
    </tr>
  <% end %>
  </table>

  <br />

<%end%>