<p>
  <b>Created By:</b>
  <%= User.find(@action_item.createdByID).formal_name %>
</p>

<p>
  <b>Approved?</b>
  <%= @action_item.isApproved %>
</p>

<%if !current_user.blank?%>
	<%if current_user.godBit or current_user.isSuperUser or current_user.id == @action_item.createdByID%>
		<%= link_to 'Edit', edit_action_item_path(@action_item) %> |
	<%end%>
<%end%>

<%= link_to 'Back', action_items_path %>
