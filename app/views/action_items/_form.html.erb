<%= form_for(@action_item) do |f| %>
  <% if @action_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@action_item.errors.count, "error") %> prohibited this action_item from being saved:</h2>

      <ul>
      <% @action_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Created By:" %><br />
      <%= f.select :createdByID, [[User.find(@action_item.createdByID).formal_name, User.find(@action_item.createdByID).id]], :readonly => true%>
  </div>

  <div class="field">
    <%= f.label :isApproved %><br />
    <%= f.check_box :isApproved %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>