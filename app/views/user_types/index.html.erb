<h1>Listing user_types</h1>

<table>
  <tr>
    <th>Userid</th>
    <th>Issuperuser</th>
    <th>Isgenericuser</th>
    <th>Isfacultyuser</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_types.each do |user_type| %>
  <tr>
    <td><%= user_type.userID %></td>
    <td><%= user_type.isSuperUser %></td>
    <td><%= user_type.isGenericUSer %></td>
    <td><%= user_type.isFacultyUser %></td>
    <td><%= link_to 'Show', user_type %></td>
    <td><%= link_to 'Edit', edit_user_type_path(user_type) %></td>
    <td><%= link_to 'Destroy', user_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User type', new_user_type_path %>
